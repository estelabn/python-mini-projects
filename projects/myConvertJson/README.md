# Convert a Json file into a csv
takes a json file as input and generate a csv file in output

### Prerequisites module:
* json
* Run 'pip install json' to install external modules

### How to run the script
- Execute  'python3 converter.py'